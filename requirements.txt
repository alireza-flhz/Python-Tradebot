# Pinned so the notebooks/scripts run without the two crashes this repo
# was hitting:
#
# 1) pandas_ta 0.3.14b0 (the last release, unmaintained) still does
#    `from numpy import NaN as npNaN`. numpy>=1.24 removed that alias,
#    so importing pandas_ta raises:
#    ImportError: cannot import name 'NaN' from 'numpy'
#    -> keep numpy below 1.24.
#
# 2) plotly's fig.show() in Jupyter/VS Code raises:
#    ValueError: Mime type rendering requires nbformat>=4.2.0 but it is
#    not installed
#    -> nbformat (and ipython, for interactive display) must be present.

numpy<1.24,>=1.23
pandas<2.1,>=1.5
pandas_ta==0.3.14b0
scipy>=1.10
plotly>=5.18
nbformat>=4.2.0
ipython>=8.12
ccxt>=4.1
yfinance>=0.2.28
backtesting>=0.3.3
